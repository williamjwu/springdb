server:
  port: 9083
spring:
  application:
    name: service-config
  cloud:
    config:
      profile: service-student, service-school, service-zuul
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9080/eureka/
management:
  endpoints:
    web:
      exposure:
        include: *